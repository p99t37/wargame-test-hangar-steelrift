# Icons

Drop weapon, upgrade, and support icons here. Reference them from `src/data.js` via the optional `icon` field on each entry, e.g. `icon: "icons/autocannon.svg"`.

The Steel Rift github has a usable set of weapon glyphs.
